{
  "name": "prompt-generator-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build && cd .. && mkdir -p public && cp -r client/dist/* public/",
    "install": "cd client && npm install"
  }
}
